import Forms from '../../containers/Forms';
import LateralBar from '../../containers/LateralBar';

const Register = () => {
  return (
    <>
      <LateralBar showFilters={false} />
      <Forms />
    </>
  );
};

export default Register;
